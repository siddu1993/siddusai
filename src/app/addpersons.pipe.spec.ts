import { AddpersonsPipe } from './addpersons.pipe';

describe('AddpersonsPipe', () => {
  it('create an instance', () => {
    const pipe = new AddpersonsPipe();
    expect(pipe).toBeTruthy();
  });
});
